using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using EasyUI.Toast;

public class sizeCanvas : MonoBehaviour
{
    // Start is called before the first frame update

    // Update is called once per frame

    void Start()
    {
        //Toast.Show("alto : " + Screen.height.ToString() + "   ancho : " + Screen.width.ToString(), 1f, ToastColor.Magenta);
        //Toast.Show("X : " + temp.transform.position.x.ToString() + "   Z : " + temp.transform.position.z.ToString(), 10f, ToastColor.Magenta);

    }
}
